# Wise statement test fixtures

PDF fixtures in this directory are **synthetically generated** — they contain
no real names, IBANs, addresses, salaries, or transaction data.

Real bank statement PDFs must NEVER be committed to this repository.
The `.gitignore` blocks `*.pdf` here as a safety net; synthetic fixtures
are explicitly allowed via negation rules.
